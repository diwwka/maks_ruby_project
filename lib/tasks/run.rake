namespace :app do
  desc "Запуск основного додатку (парсинг та збереження)"
  task :run do
    puts "--- Запуск через Rake ---"
    # Просто підключаємо main.rb, який виконає всю роботу
    # (Оскільки main.rb лежить в корені, піднімаємось на рівень вище ../../)
    require_relative '../../main'
  end
end